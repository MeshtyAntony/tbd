# Name: tbd

A simple ai agent that Works with smaller models without native tool calling

## Installation
Linux:

```bash
curl -sSL https:///install.sh | bash
```

Windows:

```bash
curl -sSL https:///install.ps1 | powershell
```
